"use client";\n\nimport { useAuth } from \"@/hooks/use-auth\";\nimport { useRouter } from \"next/navigation\";\n\n/**\n * Hook to handle account disabled responses\n * When API returns 403 with code \"ACCOUNT_DISABLED\"\n */\nexport function useAccountDisabledHandler() {\n  const { logout } = useAuth();\n  const router = useRouter();\n\n  const handleAccountDisabled = (errorCode?: string) => {\n    if (errorCode === \"ACCOUNT_DISABLED\") {\n      // Clear auth state\n      logout();\n\n      // Redirect to login with message\n      router.push(\"/login?reason=account_disabled\");\n\n      // Show notification (implement your notification system)\n      console.error(\n        \"Your account has been disabled. Please contact your administrator.\"\n      );\n    }\n  };\n\n  return { handleAccountDisabled };\n}\n"